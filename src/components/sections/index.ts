/**
 * Section Components - Composite Landing Page Sections
 *
 * Barrel export for all section components.
 * Uses 100% design tokens - no hardcoded values.
 */

export { SectionHeader, type SectionHeaderProps } from './SectionHeader';
