/**
 * Homepage Metadata
 *
 * Exports metadata for the Unite-Group home page using the SEO configuration system.
 */

import { buildMetadata } from '@/lib/seo/buildPageMetadata';

export const metadata = buildMetadata('home');
