import { NextResponse } from 'next/server';

// Placeholder stub: endpoint not yet implemented. Returns 404 to satisfy route lookup.
export async function GET() {
  return NextResponse.json(
    { error: 'skill-intelligence endpoint not implemented' },
    { status: 404 }
  );
}

export async function POST() {
  return NextResponse.json(
    { error: 'skill-intelligence endpoint not implemented' },
    { status: 404 }
  );
}
