/**
 * Core Permission Service (Stub for Build)
 * TODO: Implement full permission service
 */

export async function hasPermission(
  userId: string,
  workspaceId: string,
  resource: string,
  action: string
): Promise<boolean> {
  // Default to false for safety
  return false;
}
