# Database Health Monitoring - Environment Configuration
#
# Copy this file to ~/.unite-hub-monitoring.env and configure your integrations
# Then source it before running the monitoring script:
#   source ~/.unite-hub-monitoring.env && ./scripts/monitor-database-health.sh

# =============================================================================
# GOOGLE INTEGRATIONS (Recommended - Free Tier Available)
# =============================================================================

# Google Chat Webhooks (Slack Alternative)
# 1. Go to https://chat.google.com
# 2. Create a space: "+ Create a space" → "Unite-Hub Alerts"
# 3. Click space name → "Apps & integrations" → "+ Add webhooks"
# 4. Name: "Database Health Monitor"
# 5. Copy the webhook URL below
GOOGLE_CHAT_WEBHOOK_URL=""
# Example: https://chat.googleapis.com/v1/spaces/AAAA.../messages?key=...&token=...

# Gmail Alerts (Email Notifications)
# Set the email address where you want to receive critical alerts
# Requires mail or sendmail to be installed on your system
GMAIL_ALERT_EMAIL=""
# Example: team-alerts@your-company.com

# Google Cloud Monitoring (Datadog Alternative)
# 1. Go to https://console.cloud.google.com
# 2. Create/select a project
# 3. Enable Monitoring API: gcloud services enable monitoring.googleapis.com
# 4. Copy your project ID below
GCP_PROJECT_ID=""
# Example: my-project-123456

# =============================================================================
# LEGACY INTEGRATIONS (Optional)
# =============================================================================

# Slack Webhooks (Legacy Support)
# Only needed if you're still using Slack
SLACK_WEBHOOK_URL=""
# Example: https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX

# Datadog API (Legacy Support)
# Only needed if you're using Datadog instead of Google Cloud Monitoring
DD_API_KEY=""
# Example: 1234567890abcdef1234567890abcdef

# =============================================================================
# MONITORING CONFIGURATION
# =============================================================================

# How often to run the monitoring script (for cron)
# Example: Run every 6 hours
# 0 */6 * * * source ~/.unite-hub-monitoring.env && /path/to/Unite-Hub/scripts/monitor-database-health.sh

# Alert thresholds (configured in monitoring-agent.json):
# - Success Rate < 95% → WARNING
# - Success Rate < 90% → CRITICAL
# - Average Latency > 500ms → WARNING
# - Average Latency > 1000ms → CRITICAL
# - Circuit State = OPEN → CRITICAL
# - Circuit State = HALF_OPEN → WARNING

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file:
#    cp .env.monitoring.example ~/.unite-hub-monitoring.env

# 2. Edit with your credentials:
#    nano ~/.unite-hub-monitoring.env

# 3. Test the configuration:
#    source ~/.unite-hub-monitoring.env
#    ./scripts/monitor-database-health.sh

# 4. Set up automated monitoring (cron):
#    crontab -e
#    # Add line: 0 */6 * * * source ~/.unite-hub-monitoring.env && /absolute/path/to/Unite-Hub/scripts/monitor-database-health.sh

# 5. View monitoring reports:
#    ls -lh monitoring/reports/

# For detailed setup guides, see:
# - GOOGLE_INTEGRATIONS_SETUP.md (Google Chat, Gmail, GCP)
# - DYNAMIC_AGENT_MONITORING_GUIDE.md (Agent configuration)
# - .claude/agents/README.md (Agent usage)
