---
phase: 02-ui-components
type: execute
---

<objective>
Migrate remaining low-violation UI components to design tokens.

Purpose: Complete Phase 2 UI migration, reaching near-zero UI violations.
Output: 10 component files migrated (excluding intentional purple brand colors).
</objective>

<context>
@.planning/STATE.md
@.planning/phases/02-ui-components/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate select.tsx, popover.tsx, dropdown-menu.tsx, dialog.tsx</name>
  <files>src/components/ui/select.tsx, src/components/ui/popover.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/dialog.tsx</files>
  <action>Replace hardcoded colors:
    - slate-700/800 → bg-bg-raised, border-border-medium
    - slate-300/400 → text-text-muted, border-border-subtle
  </action>
  <verify>Files don't appear in test violations</verify>
  <done>4 files migrated</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Slider.tsx, loading-skeleton.tsx, image-comparison.tsx</name>
  <files>src/components/ui/Slider.tsx, src/components/ui/loading-skeleton.tsx, src/components/ui/image-comparison.tsx</files>
  <action>Replace hardcoded colors:
    - gray-200/700 → bg-bg-hover, text-text-muted
    - slate-700/800 → bg-bg-raised
  </action>
  <verify>Files don't appear in test violations</verify>
  <done>3 files migrated</done>
</task>

<task type="auto">
  <name>Task 3: Migrate three-d-carousel.tsx, dock.tsx, TransparencyFooter.tsx</name>
  <files>src/components/ui/three-d-carousel.tsx, src/components/ui/dock.tsx, src/components/ui/footer/TransparencyFooter.tsx</files>
  <action>Replace hardcoded colors:
    - slate-900/800 → bg-bg-base, bg-bg-raised
    - gray-900 → bg-bg-base, text-text-primary
    - green-500 → success-500
  </action>
  <verify>Files don't appear in test violations</verify>
  <done>3 files migrated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test tests/design-tokens/token-usage.test.ts` passes
- [ ] UI violations near 0 (only intentional purple exceptions)
- [ ] `npm run dev` starts without errors
</verification>

<success_criteria>
- All 10 files migrated
- UI violations ≤ 5 (purple brand exceptions only)
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-components/02-03-SUMMARY.md`
</output>
