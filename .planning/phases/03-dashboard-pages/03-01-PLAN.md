---
phase: 03-dashboard-pages
type: execute
---

<objective>
Migrate core dashboard infrastructure pages to design tokens.

Purpose: Establish design token patterns for dashboard, fix high-impact shared files.
Output: Core dashboard files migrated (page.tsx, layout.tsx, overview/, analytics/).
</objective>

<context>
@.planning/STATE.md
@.planning/phases/02-ui-components/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate dashboard/page.tsx and dashboard/layout.tsx</name>
  <files>src/app/dashboard/page.tsx, src/app/dashboard/layout.tsx</files>
  <action>Replace hardcoded colors:
    - slate-950/900/800 → bg-bg-base, bg-bg-raised
    - slate-400 → text-text-muted
    - All gradient variations → bg-gradient-* with tokens
  </action>
  <verify>Files don't appear in test violations</verify>
  <done>2 core files migrated</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dashboard/overview/ pages</name>
  <files>src/app/dashboard/overview/page.tsx and related files</files>
  <action>Replace hardcoded colors following Phase 2 patterns:
    - gray-* → text-text-*, bg-bg-*
    - slate-* → bg-bg-*, border-border-*
    - red-* → error-*
    - green-* → success-*
    - blue-* → info-*
    - yellow/amber-* → warning-*
  </action>
  <verify>Files don't appear in test violations</verify>
  <done>Overview pages migrated</done>
</task>

<task type="auto">
  <name>Task 3: Migrate dashboard/analytics/ pages</name>
  <files>src/app/dashboard/analytics/page.tsx and related files</files>
  <action>Replace hardcoded colors following established patterns</action>
  <verify>Files don't appear in test violations</verify>
  <done>Analytics pages migrated</done>
</task>

<task type="auto">
  <name>Task 4: Migrate dashboard/modern/ pages</name>
  <files>src/app/dashboard/modern/page.tsx and related files</files>
  <action>Replace hardcoded colors following established patterns</action>
  <verify>Files don't appear in test violations</verify>
  <done>Modern dashboard pages migrated</done>
</task>

</tasks>

<token_mapping>
Established patterns from Phase 2:
- `slate-950` → `bg-bg-base`
- `slate-900` → `bg-bg-raised`
- `slate-800` → `bg-bg-card`
- `slate-700` → `border-border-medium`
- `slate-400/500` → `text-text-muted`
- `slate-300` → `text-text-secondary`
- `gray-900` → `text-text-primary`, `bg-bg-base`
- `gray-600/700` → `text-text-secondary`
- `gray-400/500` → `text-text-muted`
- `gray-200/300` → `border-border-subtle`, `bg-bg-hover`
- `red-*` → `error-*`
- `green-*` / `emerald-*` → `success-*`
- `blue-*` → `info-*`
- `yellow-*` / `amber-*` → `warning-*`
- `orange-*` → `accent-*`
- `purple-*` → keep (brand differentiation)
</token_mapping>

<verification>
Before declaring plan complete:
- [ ] `npm test tests/design-tokens/token-usage.test.ts` shows reduced page violations
- [ ] `npm run dev` starts without errors
- [ ] Dashboard pages render correctly
</verification>

<success_criteria>
- Core dashboard infrastructure migrated
- No visual regressions
- Reduced page violation count
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-pages/03-01-SUMMARY.md`
</output>
