---
phase: 03-dashboard-pages
plan: 03-02
type: execute
---

<objective>
Migrate dashboard tool pages to design tokens.

Purpose: Reduce violations in AI/intelligence tool pages.
Output: 7 files migrated (~184 violations).
</objective>

<context>
@.planning/STATE.md
@.planning/phases/03-dashboard-pages/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ai-tools/ pages</name>
  <files>
    - src/app/dashboard/ai-tools/page.tsx (2 violations)
    - src/app/dashboard/ai-tools/code-generator/page.tsx (20 violations)
    - src/app/dashboard/ai-tools/marketing-copy/page.tsx (35 violations)
  </files>
  <action>Replace hardcoded colors following established patterns:
    - slate-* → bg-bg-*, border-border-*, text-text-*
    - gray-* → text-text-*, bg-bg-*
    - blue-* → info-*
    - green-* → success-*
    - red-* → error-*
    - yellow-* → warning-*
    - text-white → text-text-primary
  </action>
  <verify>Files don't appear in test violations</verify>
  <done>3 ai-tools files migrated (~57 violations)</done>
</task>

<task type="auto">
  <name>Task 2: Migrate insights/ pages</name>
  <files>
    - src/app/dashboard/insights/page.tsx (19 violations)
    - src/app/dashboard/insights/competitors/page.tsx (28 violations)
  </files>
  <action>Replace hardcoded colors following established patterns</action>
  <verify>Files don't appear in test violations</verify>
  <done>2 insights files migrated (~47 violations)</done>
</task>

<task type="auto">
  <name>Task 3: Migrate intelligence/ page</name>
  <files>src/app/dashboard/intelligence/page.tsx (27 violations)</files>
  <action>Replace hardcoded colors following established patterns</action>
  <verify>File doesn't appear in test violations</verify>
  <done>Intelligence page migrated</done>
</task>

<task type="auto">
  <name>Task 4: Migrate monitoring/ page</name>
  <files>src/app/dashboard/monitoring/page.tsx (53 violations)</files>
  <action>Replace hardcoded colors following established patterns</action>
  <verify>File doesn't appear in test violations</verify>
  <done>Monitoring page migrated</done>
</task>

</tasks>

<token_mapping>
Established patterns from Phase 2 and Plan 03-01:
- `slate-950` → `bg-bg-base`
- `slate-900` → `bg-bg-raised`
- `slate-800` → `bg-bg-card`
- `slate-700` → `border-border-medium`
- `slate-600` → `border-border-subtle`
- `slate-400/500` → `text-text-muted`
- `slate-300` → `text-text-secondary`
- `gray-900` → `text-text-primary`, `bg-bg-base`
- `gray-600/700` → `text-text-secondary`
- `gray-400/500` → `text-text-muted`
- `gray-200/300` → `border-border-subtle`, `bg-bg-hover`
- `text-white` → `text-text-primary`
- `red-*` → `error-*`
- `green-*` / `emerald-*` → `success-*`
- `blue-*` → `info-*`
- `yellow-*` / `amber-*` → `warning-*`
- `orange-*` → `accent-*`
- `purple-*` → keep (brand differentiation)
</token_mapping>

<verification>
Before declaring plan complete:
- [ ] `npm test tests/design-tokens/token-usage.test.ts` shows reduced page violations
- [ ] `npm run dev` starts without errors
- [ ] Dashboard tool pages render correctly
</verification>

<success_criteria>
- All 7 dashboard tool files migrated
- ~184 violations reduced
- No visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-pages/03-02-SUMMARY.md`
</output>
